import { Meta } from "@storybook/blocks";

<Meta title="Neon UI/Introduction" />

<header className="storybook-global-banner">
  <div className="storybook-global-banner__content">
    <p className="storybook-global-banner__eyebrow">Neon UI documentation</p>
    <h1 className="storybook-global-banner__title">Neon UI</h1>
  </div>
  <div className="storybook-global-banner__meta" aria-label="Design system qualities" role="list">
    <span role="listitem">Sapphire Trust</span>
    <span role="listitem">Light and dark</span>
    <span role="listitem">Token driven</span>
  </div>
</header>

<div className="intro-wrapper">
    
    # Neon UI design

    A production-ready React component library with typed tokens, accessible interactions, and a Sapphire Trust theme built for professional product interfaces.

    This library provides production-oriented React components, CSS custom properties, and TypeScript tokens for frontend teams building product interfaces.

    ## Principles

    - Keep controls predictable, compact, and accessible.
    - Prefer native semantics where possible.
    - Use stable dimensions for buttons, inputs, tabs, and overlays.
    - Treat Storybook as the shared source of truth for states and variants.

    ## Import

    ```tsx
    import { Button, TextField, Select } from "neon-ui-lib";
    ```

    ## CSS

    Import once at the app root if your bundler does not automatically include package CSS:

    ```tsx
    import "neon-ui-lib/styles.css";
    ```

</div>
